{
  "name": "qingping-webhook",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --log-level=debug",
    "start": "wrangler dev --log-level=info"
  },
  "devDependencies": {
    "wrangler": "^3.60.3"
  }
}